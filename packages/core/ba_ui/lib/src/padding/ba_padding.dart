import 'package:flutter/material.dart';

class BaPadding {
  BaPadding._();

  static const xxlAll = EdgeInsets.all(40);
  static const xlAll = EdgeInsets.all(30);
  static const lgAll = EdgeInsets.all(20);
  static const mdAll = EdgeInsets.all(15);
  static const smAll = EdgeInsets.all(10);
  static const xsAll = EdgeInsets.all(5);

  static const zero = EdgeInsets.zero;

  static const xxlHorizontal = EdgeInsets.symmetric(horizontal: 40);
  static const xlHorizontal = EdgeInsets.symmetric(horizontal: 30);
  static const lgHorizontal = EdgeInsets.symmetric(horizontal: 20);
  static const mdHorizontal = EdgeInsets.symmetric(horizontal: 15);
  static const smHorizontal = EdgeInsets.symmetric(horizontal: 10);
  static const xsHorizontal = EdgeInsets.symmetric(horizontal: 5);

  static const xxlVertical = EdgeInsets.symmetric(vertical: 40);
  static const xlVertical = EdgeInsets.symmetric(vertical: 30);
  static const lgVertical = EdgeInsets.symmetric(vertical: 20);
  static const mdVertical = EdgeInsets.symmetric(vertical: 15);
  static const smVertical = EdgeInsets.symmetric(vertical: 10);
  static const xsVertical = EdgeInsets.symmetric(vertical: 5);
}
